import { lintFormatterConfig } from './lint-formatter-config';

describe('lintFormatterConfig', () => {
  it('should work', () => {
    expect(lintFormatterConfig()).toEqual('lint-formatter-config');
  });
});
